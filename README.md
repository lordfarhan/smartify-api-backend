# Install
```
    # composer install / update
    # copy .env.example .env // duplicate file .env.example to .env
    # php artisan key:generate
    
    create database and set to the env files
    
    # php artisan migrate // create tables
    # php artisan jwt-secret -> copy the key and paste to .env -> jwt-secret
```